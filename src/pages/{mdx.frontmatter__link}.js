import * as React from "react";
import Layout from "../components/Layout";
import { graphql } from "gatsby";

export default function ProjectPost() {
  return (
    <Layout>
      <p>My project post contents will go here (eventually).</p>
    </Layout>
  );
}